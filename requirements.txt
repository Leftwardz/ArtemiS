# ArtemiS — dependências diretas (Windows: runtime + build no mesmo venv).
# Python 3.10+ (testado em 3.11, 3.12 e 3.14). Instalar: pip install -r requirements.txt
#
# Removidos em relação à lista antiga (não importados em app/):
#   scipy, Eel, gevent*, greenlet, zope.*, fastnumbers, Levenshtein/rapidfuzz,
#   code128, datamatrix, json-tricks, pdf2image, tomlkit, wcwidth, future
# Pacotes só de build/docs: pyinstaller, markdown (scripts em scripts/).

# --- UI ---
customtkinter>=5.2,<7

# --- PDF / imagens / códigos de barras ---
Pillow>=10,<13
reportlab>=4,<5
PyPDF2>=3.0,<4
pylibdmtx>=0.1.10
PyQRCode>=1.2.1
pypng>=0.20220715.0
python-barcode>=0.15

# --- dados / sistema ---
SQLAlchemy>=2.0,<3
screeninfo>=0.8
psutil>=5.9

# --- Windows (impressão, AD, spooler) ---
pywin32>=306

# pylibdmtx ainda usa distutils (removido do stdlib no 3.12+)
setuptools>=65

# --- build (PyInstaller → dist/Main.exe) ---
pyinstaller>=6,<7

# --- scripts opcionais (manuais em docs/user/) ---
markdown>=3.5
